# 截图清单（由你截取，共 4 张）

把截好的图片放进本目录（`screenshots/`），文件名按下表命名，README 会引用它们。

| # | 建议文件名 | 截什么 | 画面要点 |
| --- | --- | --- | --- |
| 1 | `01_dev_codex.png` | Codex 会话界面（开发过程） | 能看出"用 AI 工具协作开发"：本任务的会话里包含方案对齐、执行命令、跑测试的片段 |
| 2 | `02_dev_terminal.png` | 终端跑测试与流水线 | 能看到命令 `python -m unittest discover -s tests -t .` 与 `python run_eval.py --mode llm`，以及输出里的"整体加权分""最差 3 条" |
| 3 | `03_result_overview.png` | HTML 报告总览 | 浏览器打开 `outputs/llm_report.html`，截"整体加权分卡片 + 各指标分布"部分 |
| 4 | `04_result_worst_cases.png` | 最差 3 条分析 | 同一页面向下滚动，截"最差 3 条及分析"部分（含评分与证据句） |

## 小提示

1. 截图前把窗口最大化、字号调大，确保中文字迹清晰。
2. 终端里先运行一次 `python run_eval.py --mode llm`，输出会保留在滚动区，方便重复截图；同样的内容也保存在 `outputs/llm_console.txt`。
3. 如果希望连"开发过程"更像真实开发，可再补一张 `05_dev_tests.png`：终端里 31 项单测全部通过的画面。
